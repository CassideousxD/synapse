# ADR 0001: Agent runtime runs client-side (TypeScript, Web Worker)
Notes and agent checkpoints live in IndexedDB, so the draft/review/revise loop
and the ask-the-student pause/resume state live in the browser too.
